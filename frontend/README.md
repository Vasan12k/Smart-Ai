# IPO Frontend

This is the React + Vite frontend for IPO. It uses Tailwind CSS.

Quick start:

1. cd frontend
2. npm install
3. npm run dev

The app is mobile-first and responsive. Continue building role-specific pages under `src/pages`.
